server:
  address: 0.0.0.0
  port: ${INVENTORY_SERVER_PORT}

spring:
  application:
    name: inventory-service

  output:
    ansi:
      enabled: always

  profiles:
    active: ${SPRING_PROFILES_ACTIVE}
